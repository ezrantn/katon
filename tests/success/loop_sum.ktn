func main(n: nat) {
    let i: nat = 0;
    let sum: nat = 0;
    
    while i < n {
        invariant sum >= 0; // The solver must prove this holds every iteration
        sum = sum + i;
        i = i + 1;
    }
    
    ensures sum >= 0;
}